import 'package:flutter/material.dart';

class EquiposScreen extends StatelessWidget {
  const EquiposScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('EquiposScreen'),
      ),
    );
  }
}
