import 'package:flutter/material.dart';

class PilotosScreen extends StatelessWidget {
  const PilotosScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('PilotosScreen'),
      ),
    );
  }
}
